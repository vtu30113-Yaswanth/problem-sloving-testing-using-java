class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        result=0
        ans=[]
        for num in nums:
            result+=num
            ans.append(result)
        return ans
